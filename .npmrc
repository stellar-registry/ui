# Enforce engines.npm — CI and local installs must use the same npm major
# version, or a lockfile written by one can be rejected as out-of-sync by
# the other's `npm ci` (see ci.yml's npm bump and package.json's engines).
engine-strict=true
